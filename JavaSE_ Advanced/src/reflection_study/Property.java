package reflection_study;

/**
 * @author MOYU
 * @File JavaSE_Study-reflection_study
 * @Date 2022/6/26 20:36
 **/
public class Property {
    /*
    反射是指对于任何一个Class类，在“运行时都可以直接的得到这个类的全部成分
    关键：
        先得到编译后的Class类对象，然后得到Class的全部成分

     反射的作用：
               可以在运行时得到一个类的全部成分然后操作
               可以破坏封装性
               破坏泛型的约束(绕过编译阶段为集合添加数据)
               做Java高级框架
    */
}
